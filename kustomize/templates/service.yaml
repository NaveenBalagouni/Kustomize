apiVersion: v1
kind: Service
metadata:
  name: {{ include "kustomize.fullname" . }}
  labels:
    {{- include "kustomize.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kustomize.selectorLabels" . | nindent 4 }}
