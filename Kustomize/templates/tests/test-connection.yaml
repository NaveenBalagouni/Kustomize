apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Kustomize.fullname" . }}-test-connection"
  labels:
    {{- include "Kustomize.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Kustomize.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
